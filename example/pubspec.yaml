name: page_turn_animation_example
description: Demonstrates the page_turn_animation package with an interactive book viewer.
publish_to: "none"

environment:
  sdk: ^3.10.1
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  page_turn_animation:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
